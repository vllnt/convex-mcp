# @vllnt/convex-mcp — Full LLM Context

> Expose Convex functions as MCP (Model Context Protocol) tools via Streamable HTTP.
> This file contains everything needed to use the package correctly.

## Install

```bash
pnpm add @vllnt/convex-mcp
```

Peer dependency: `convex` (>=1.0.0)

---

## Core Concepts

1. You define Convex functions (queries, mutations, actions) in your Convex project.
2. You create an MCP server config that maps those functions to named tools.
3. You mount the server on a route handler (Next.js App Router, Cloudflare Workers, etc.).
4. LLM clients connect via Streamable HTTP and call your Convex functions as MCP tools.

---

## Complete API Reference

### createMCPServer(config) -> ConvexMCPServer

Creates an MCP server. Auth is REQUIRED (default-deny — throws without it).

```typescript
import { createMCPServer } from "@vllnt/convex-mcp";

const mcp = createMCPServer({
  // REQUIRED: auth configuration
  auth: {
    // REQUIRED: validate API key from Authorization: Bearer <key> header
    validate: (apiKey: string) => Promise<boolean> | boolean,

    // OPTIONAL: return a Convex auth token (JWT) to set on ConvexHttpClient
    // This populates ctx.auth in your Convex functions
    convexToken?: (apiKey: string) => Promise<string | undefined> | string | undefined,
  },

  // OPTIONAL: named MCP tools. Keys become tool names.
  tools?: Record<string, ToolDef>,

  // OPTIONAL: named MCP resources. Keys are URI template patterns.
  resources?: Record<string, ResourceDef>,

  // OPTIONAL: Convex deployment URL
  // Falls back to CONVEX_URL then NEXT_PUBLIC_CONVEX_URL env var
  convexUrl?: string,

  // OPTIONAL: MCP server name (default: "convex-mcp")
  name?: string,

  // OPTIONAL: MCP server version (default: "0.3.1")
  version?: string,

  // OPTIONAL: pagination and two-phase discovery
  pagination?: PaginationConfig,
});
```

Throws:
- If `auth.validate` is not provided.
- If no Convex URL is found.
- If `pagination.pageSize` is not a positive integer >= 1.

### .handler(options?) -> { GET, POST }

Returns request handlers compatible with Next.js App Router route handlers.

```typescript
const { GET, POST } = mcp.handler();
```

Both GET and POST accept a Request and return Promise<Response>.
POST validates Content-Type: application/json (returns 415 otherwise).

### query(ref, options?) -> ToolDef

Wraps a Convex query function reference as an MCP tool.

```typescript
import { query } from "@vllnt/convex-mcp";

query(api.tasks.list, {
  args: v.object({ status: v.optional(v.string()) }),
  description: "List tasks by status",
})
```

### mutation(ref, options?) -> ToolDef

Wraps a Convex mutation. Same signature as query().

```typescript
import { mutation } from "@vllnt/convex-mcp";

mutation(api.tasks.create, {
  args: v.object({ title: v.string() }),
  description: "Create a task",
})
```

### action(ref, options?) -> ToolDef

Wraps a Convex action. Same signature as query().

```typescript
import { action } from "@vllnt/convex-mcp";

action(api.ai.summarize, {
  args: v.object({ documentId: v.id("documents") }),
  description: "Summarize a document",
})
```

### resource(ref, options?) -> ResourceDef

Wraps a Convex query as an MCP resource with a URI template.

```typescript
import { resource } from "@vllnt/convex-mcp";

// Used as a key in the resources record:
resources: {
  "space://{id}": resource(api.spaces.get, {
    args: v.object({ id: v.id("spaces") }),
    description: "Get a space by ID",
  }),
}
```

### convexArgsToZod(argsValidator) -> z.ZodObject

Converts a Convex v.object() validator to a Zod object schema. Throws if not v.object().

### convertValidator(validator) -> z.ZodTypeAny

Converts any single Convex validator to a Zod schema. Recursive.
Throws UnsupportedValidatorError for unknown validator kinds.

### UnsupportedValidatorError

```typescript
import { UnsupportedValidatorError } from "@vllnt/convex-mcp";
// Error: 'Unsupported Convex validator kind: "futureType". This may be from a newer version of Convex.'
```

---

## Pagination + Two-Phase Discovery

Opt-in features for servers with many tools. Reduces token overhead and supports cursor-based pagination.

### PaginationConfig

```typescript
interface PaginationConfig {
  pageSize: number;              // Tools per page (positive integer >= 1)
  twoPhaseDiscovery?: boolean;   // Enable tools/list_summary + tools/describe (default: false)
}
```

### Cursor-Based Pagination (MCP Spec-Compliant)

When `pagination` is set in config:

- `tools/list` WITHOUT cursor: returns ALL tools (backwards-compatible, no behavior change)
- `tools/list` WITH `cursor: ""`: starts pagination, returns first `pageSize` tools + `nextCursor`
- `tools/list` WITH `cursor: "<nextCursor>"`: returns next page + `nextCursor` (or absent on last page)
- Invalid/tampered cursors return JSON-RPC error -32602

Cursors are HMAC-signed with a per-instance secret (constant-time verification). They expire naturally on server restart.

### Two-Phase Discovery (Non-Standard)

When `twoPhaseDiscovery: true`:

- `tools/list_summary`: returns `{ tools: [{ name, description }] }` — no `inputSchema`, ~90% token reduction
- `tools/describe` with `{ name: "toolName" }`: returns full tool definition including `inputSchema`
- Unknown tool name returns JSON-RPC error -32602

These are non-standard MCP methods (community proposal #1923). Only custom agents that explicitly call these methods will benefit. Standard MCP clients (Claude Desktop, Cursor, etc.) will not use them.

### Example with Pagination

```typescript
const mcp = createMCPServer({
  auth: { validate: async (key) => key === process.env.MCP_API_KEY },
  tools: {
    // 50+ tools...
  },
  pagination: {
    pageSize: 20,
    twoPhaseDiscovery: true,
  },
});
```

### Exported Types

```typescript
import type { PaginationConfig, ToolSummary, ToolPage } from "@vllnt/convex-mcp";

interface ToolSummary {
  name: string;
  description: string;
}

interface ToolPage<T extends object> {
  tools: T[];
  nextCursor?: string;
}
```

---

## Types

```typescript
interface ServerConfig {
  auth: AuthConfig;
  tools?: Record<string, ToolDef>;
  resources?: Record<string, ResourceDef>;
  convexUrl?: string;
  name?: string;
  version?: string;
  pagination?: PaginationConfig;
  hooks?: LifecycleHooks;          // v0.3.0+
}

interface PaginationConfig {
  pageSize: number;
  twoPhaseDiscovery?: boolean;
}

interface AuthConfig {
  validate: (apiKey: string) => Promise<boolean> | boolean;
  convexToken?: (apiKey: string) => Promise<string | undefined> | string | undefined;
}

interface ToolDef {
  ref: unknown;                    // Convex function reference (e.g. api.tasks.list)
  type: "query" | "mutation" | "action";
  args?: ConvexValidator;          // Convex v.object() validator
  description?: string;
  tags?: Record<string, string>;   // arbitrary metadata visible in CallContext.toolDef.tags
  timeout?: number;                // ms before the dispatch is aborted
  onError?: (ctx: CallContext & { phase: "error" }) => Promise<OnCallResult | void> | OnCallResult | void;
}

interface ResourceDef {
  ref: unknown;                    // Convex function reference (must be a query)
  args?: ConvexValidator;
  description?: string;
}

interface LifecycleHooks {
  onToolCall?: (ctx: CallContext) => Promise<OnCallResult | void> | OnCallResult | void;
}

interface CallContext {
  requestId: string;               // crypto.randomUUID(), shared across before/success/error for one tool call
  toolName: string;
  toolDef: Omit<ToolDef, "ref" | "onError">;
  args: Record<string, unknown>;
  apiKey: string | undefined;
  phase: "before" | "success" | "error";
  result?: unknown;                // present on "success"
  error?: unknown;                 // present on "error"
  durationMs?: number;             // present on "success" and "error"
  startedAt: number;
}

interface OnCallResult {
  abort?: boolean;                 // before phase — skip dispatch, return error response
  errorMessage?: string;           // before phase — message when aborting (default: "Tool call rejected")
  message?: string;                // error phase — replace default error text ("Function execution failed")
  extendArgs?: Record<string, unknown>;  // v0.3.0+ — merge into dispatched args; server-side wins on collision
}

interface ConvexMCPServer {
  handler: () => {
    GET: (request: Request) => Promise<Response>;
    POST: (request: Request) => Promise<Response>;
  };
}

type FunctionType = "query" | "mutation" | "action";
```

---

## Lifecycle Hooks & Request Context Propagation

The `onToolCall` hook fires three times per tool call: `before` (pre-dispatch), `success` (after the dispatched function resolves), and `error` (after it throws). The `before` phase is the only one whose return value affects dispatch.

### Hook return shape (`OnCallResult`)

| Field | Phase | Effect |
|-------|-------|--------|
| `abort` | `before` | When `true`, framework returns an error response without dispatching. |
| `errorMessage` | `before` | Custom abort message. Default: `"Tool call rejected"`. |
| `message` | `error` | Replaces default `"Function execution failed"` text. |
| `extendArgs` | `before` | (v0.3.0+) Server-resolved key/value pairs merged into the dispatched function's args. Server-side wins on collision. Empty / undefined is a no-op. |

### Per-tool `onError` override

Tools can declare their own error hook via `ToolDef.onError`. When set, it runs INSTEAD OF `LifecycleHooks.onToolCall` for the `error` phase of that tool. Useful for per-tool error redaction.

### Request Context Propagation (v0.3.0)

The `extendArgs` field unblocks server-side context injection. Common patterns:

```typescript
// Per-action authorization with defense-in-depth
hooks: {
  onToolCall: async ({ apiKey, phase, toolDef }) => {
    if (phase !== "before") return;
    const validated = await validateKey(apiKey);
    if (!validated.valid) return { abort: true, errorMessage: "Invalid key" };
    const required = toolDef.tags?.requiredScope;
    if (required && !validated.scopes.includes(required)) {
      return { abort: true, errorMessage: `Missing scope: ${required}` };
    }
    return {
      extendArgs: {
        _mcp_apiKey: apiKey,
        _mcp_scopes: validated.scopes,
      },
    };
  },
}
```

The dispatched action's validator must declare the injected fields:

```typescript
export const upsertThing = action({
  args: {
    _mcp_apiKey: v.string(),     // injected by framework
    _mcp_scopes: v.array(v.string()),
    targetId: v.id("things"),
  },
  handler: async (ctx, args) => {
    // Defense-in-depth: re-validate the injected key inside the action.
    // Even if the framework hook is bypassed, the action stays safe.
    await assertMcpAuth(ctx, args._mcp_apiKey, "things:write");
    // ... real logic with confidence the caller is authorized ...
  },
});
```

### Other patterns

| Pattern | `extendArgs` | Action receives |
|---------|--------------|-----------------|
| Request tracing | `{ _mcp_requestId: ctx.requestId }` | Threads framework's requestId into action audit logs |
| Multi-tenancy | `{ _mcp_tenantId: resolveTenant(apiKey) }` | Server-resolved tenant ID enforced via index queries |
| Per-key feature flags | `{ _mcp_flags: getFlagsFor(apiKey) }` | Branches behavior per caller |
| Per-key quotas | `{ _mcp_quotaRemaining: n }` | Enforces or warns near limit |
| Audit metadata | `{ _mcp_callerLabel: keyMetadata.label }` | Recorded in domain audit log |

### Reserved `_` prefix (v0.3.0)

Arg keys starting with `_` are framework-controlled. Two protection layers:

1. **Schema strip** — `prepareTools` removes `_*` keys from the published JSON Schema. The MCP SDK's Zod validator silently strips `_*` keys from incoming requests (Zod default strip mode).
2. **Handler reject** — The registered tool handler explicitly rejects any `_*` key reaching it with `"Reserved arg keys not allowed: ..."` before the hook runs. Defense-in-depth for non-SDK transports.

Hook-supplied `_*` keys via `extendArgs` are exempt — the server is trusted.

### Anti-patterns

- DON'T put long-lived secrets in `extendArgs` — values flow into args, may end up in action logs.
- DON'T rely solely on the hook for authorization — without action-side re-validation, a framework regression becomes a security blind spot.
- DON'T spread context across `extendArgs` AND a side-channel store — pick one. The args path is canonical.
- DON'T allow client `_*` passthrough — the reject is what makes `extendArgs` safe.

---

## Validator Mapping (Convex -> JSON Schema)

All Convex v.* types with their JSON Schema output:

| Convex | JSON Schema | Notes |
|--------|-------------|-------|
| v.string() | { "type": "string" } | |
| v.number() / v.float64() | { "type": "number" } | v.number() is alias for v.float64() |
| v.boolean() | { "type": "boolean" } | |
| v.null() | { "type": "null" } | |
| v.int64() | { "type": "string", "description": "64-bit integer as string..." } | JSON can't represent BigInt |
| v.bytes() | { "type": "string", "description": "Binary data as base64-encoded string" } | |
| v.id("table") | { "type": "string", "description": "Convex document ID for table 'table'" } | |
| v.literal("x") | { "const": "x" } | String, number, or boolean |
| v.array(v.X()) | { "type": "array", "items": X } | Recursive |
| v.object({...}) | { "type": "object", "properties": {...}, "required": [...] } | Recursive |
| v.union(v.literal("a"), v.literal("b")) | { "enum": ["a", "b"] } | All-literal union -> enum |
| v.union(v.string(), v.number()) | { "anyOf": [...] } | Mixed union -> anyOf |
| v.optional(v.X()) | Removes field from "required" array | |
| v.record(k, v) | { "type": "object", "additionalProperties": V } | |
| v.any() | {} | Empty schema, accepts anything |

Unknown validator kinds throw UnsupportedValidatorError.

---

## Auth Flow

```
Request arrives with Authorization: Bearer <key>
  -> Extract API key from header
  -> Call auth.validate(apiKey)
     -> false: return 401 { "error": "Invalid API key." }
     -> true: continue
  -> If auth.convexToken defined: call auth.convexToken(apiKey)
     -> If returns token: ConvexHttpClient.setAuth(token)
  -> Process MCP request
```

Missing Authorization header -> 401 { "error": "Missing API key. Provide an Authorization header." }

---

## Error Handling

All Convex function errors are caught and replaced with a generic message:

Tool call errors:
```json
{ "content": [{ "type": "text", "text": "Function execution failed" }], "isError": true }
```

Resource read errors:
```json
{ "text": "{\"error\":\"Resource read failed\"}", "mimeType": "application/json" }
```

Original error messages are NEVER forwarded to the MCP client (may contain PII).

To surface specific errors, return them as part of your function's success response:
```typescript
// In your Convex function:
if (invalid) return { success: false, error: "Title required" };
return { success: true, id };
```

---

## Complete Example

```typescript
// convex/mcp.ts
import { createMCPServer, query, mutation, action, resource } from "@vllnt/convex-mcp";
import { api } from "./_generated/api";
import { v } from "convex/values";

export const mcp = createMCPServer({
  auth: {
    validate: async (key) => key === process.env.MCP_API_KEY,
    convexToken: async (apiKey) => {
      // Optional: exchange API key for Convex JWT
      return undefined;
    },
  },
  tools: {
    // Query tool (read-only, cached)
    list_tasks: query(api.tasks.list, {
      args: v.object({
        status: v.optional(v.union(
          v.literal("todo"),
          v.literal("in_progress"),
          v.literal("done"),
        )),
      }),
      description: "List tasks, optionally filtered by status",
    }),

    // Mutation tool (write, transactional)
    create_task: mutation(api.tasks.create, {
      args: v.object({
        title: v.string(),
        description: v.optional(v.string()),
        tags: v.optional(v.array(v.string())),
      }),
      description: "Create a new task",
    }),

    // Action tool (side effects, external APIs)
    summarize: action(api.ai.summarize, {
      args: v.object({
        documentId: v.id("documents"),
      }),
      description: "Generate an AI summary of a document",
    }),
  },
  resources: {
    "task://{id}": resource(api.tasks.get, {
      args: v.object({ id: v.id("tasks") }),
      description: "Get a task by ID",
    }),
  },
  // Optional: pagination for large tool catalogs
  pagination: {
    pageSize: 20,
    twoPhaseDiscovery: true,
  },
});
```

```typescript
// app/api/mcp/route.ts
import { mcp } from "@/convex/mcp";
export const { GET, POST } = mcp.handler();
```

Environment variables:
```
CONVEX_URL=https://your-deployment.convex.cloud
MCP_API_KEY=your-secret-key
```

---

## Deployment Notes

- Works with any platform supporting Web Fetch API (Request -> Response)
- Next.js App Router: export { GET, POST } from handler()
- Vercel Hobby: 10s timeout (fine for queries/mutations)
- Vercel Pro: 60s timeout
- Vercel Pro + Fluid Compute: 800s timeout (for long-running actions)
- Set maxDuration in route file: `export const maxDuration = 60;`

---

## Security Summary

- Default-deny: auth.validate is REQUIRED, server throws without it
- Generic errors: Convex errors never leak to MCP client
- HMAC-signed cursors: pagination cursors are signed with per-instance secret, constant-time verified
- No function-level authz in v1: valid API key = access to all exposed tools
- Service account model: by default ctx.auth is null; use convexToken hook for identity
- Scope tools carefully: only expose functions the key holder should access

---

## Known Limitations

1. Validator duplication: must provide v.object() validators alongside function refs (FunctionReference has no runtime schema)
2. No per-tool authorization: a valid key accesses all tools (create separate servers for different access levels)
3. Service account model: no per-user identity by default (use convexToken for identity propagation)
4. No built-in rate limiting: use platform-level rate limiting
5. Two-phase discovery is non-standard: only custom agents benefit (not Claude Desktop, Cursor, etc.)
